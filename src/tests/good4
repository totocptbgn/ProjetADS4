total = 0;
tailleX = 10;
tailleY = 5;
Y = 0;
While (Y < tailleY)
Do
	X = 0;
	If (Y > 0)
	Then
		Tourner(-1);
	End;
	While (X < tailleX)
	Do
		total = (total + Lire);
		X = ( X + 1);
		Avancer(1);
	End;
	Tourner(1);
	Avancer(1);
	Y = (Y + 1);
End;
Ecrire(total);